# ============================================================================
# .env.test — konfiguracija za Playwright E2E beta-tester
# ============================================================================
#
# Kopiraj ovaj fajl u `.env.test` i popuni prave vrednosti.
# **NE commit-uj .env.test** — sadrži service_role key koji nikad ne sme
# da izađe iz tvog mašine. .gitignore ga blokira.
# ============================================================================

# Supabase config — isti kao u .env
VITE_SUPABASE_URL=https://zrjqateswwyeoyfdjswv.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_uVgGYirSxfik7S9wbHtoYg_fHJWEUL5

# Service-role key — samo lokalno, za DB verify + test fixture reset
# Dobija se iz Supabase Dashboard → Project Settings → API → service_role secret
SUPABASE_SERVICE_ROLE_KEY=eyJ... (paste real key here)

# Test user credentials — kreiran u Supabase Dashboard → Authentication → Users
E2E_TEST_USER_EMAIL=beta@fitbyivana.test
E2E_TEST_USER_PASSWORD=BetaFit123!
E2E_TEST_USER_ID=00000000-0000-0000-0000-000000000000
